package com.kodilla.good.patterns.challenges.food2door2;

public class SupplierOrderService implements OrderService {

    public boolean order(Supplier supplier, String product, double quantity) {
        if(quantity > 0) {
        }
        return true;
    }
}